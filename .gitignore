# --- Python ---
__pycache__/
.venv/
venv/
*.pyc
.pytest_cache/
# .python-version  <-- REMOVED FROM IGNORE (Commit this)

# --- Build & Packaging ---
build/
dist/
localpycs/
# FoundryL10n.spec <-- REMOVED FROM IGNORE (Commit this)
Analysis-00.toc
EXE-00.toc
PKG-00.toc
PYZ-00.toc

# --- App Data (NEVER COMMIT) ---
*.db
*.db-journal
out/
reports/

# --- Project Resources (Ignore personal work, but keep folder structure) ---
# We ignore the actual files, but you should commit 'sample' versions
glossary.tsv
style.md
forbidden.txt

# --- Profiles ---
# Ignore your personal profiles, but keep the directory
profiles/*.json
!profiles/sample_profile.json
!profiles/.gitkeep

# --- Models ---
models/
*.gguf
*.bin
*.safetensors
# Modelfile <-- REMOVED FROM IGNORE (Commit this)

# --- IDEs ---
.vscode/
.idea/

# --- Tests & Samples ---
tests/*.tsv
tests/*.csv
tests/*.txt
!tests/__init__.py
tests/*.md
*.egg-info/
.continue/
